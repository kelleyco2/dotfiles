#!/bin/sh

set -eu
. "$HOME/.local/bin/_session-lib.sh"

TARGET=$(project_dirs | fzf --header-first --header="Launch Project" --prompt="🔮 " --preview "eza --tree --icons --level 3 --git-ignore {}")
[ -n "$TARGET" ] || exit 0

smug_launch "$(basename "$TARGET")" "$TARGET"
