session: ozone
root: ~/Code/ozone/

windows:
  - name: claude
    commands:
      - claude --continue || claude
  - name: nvim
    commands:
      - nvim
  - name: dev
    layout: even-horizontal
    commands:
      - clear
    panes:
      - type: horizontal
        commands:
          - ui
